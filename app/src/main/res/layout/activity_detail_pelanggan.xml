<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_kuning"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Detail Pelanggan"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="#333333"
            android:gravity="center"
            android:layout_marginBottom="16dp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@android:color/white"
            android:elevation="4dp"
            android:padding="16dp"
            android:layout_marginBottom="16dp"
            android:layout_gravity="center"
            android:layout_marginHorizontal="4dp">

            <TextView android:text="Nama:" style="@style/LabelText" />
            <TextView android:id="@+id/txtDetailNama" style="@style/ValueText" />

            <TextView android:text="Nomor HP:" style="@style/LabelText" />
            <TextView android:id="@+id/txtDetailNoHp" style="@style/ValueText" />

            <TextView android:text="Alamat:" style="@style/LabelText" />
            <TextView android:id="@+id/txtDetailAlamat" style="@style/ValueText" />
        </LinearLayout>
    </LinearLayout>
</ScrollView>
